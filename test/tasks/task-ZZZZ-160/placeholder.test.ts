import { describe, it, expect } from "vitest";

describe("disposable e2e fixture placeholder", () => {
  it("is a trivially-true placeholder, not a real test", () => {
    expect(true).toBe(true);
  });
});
